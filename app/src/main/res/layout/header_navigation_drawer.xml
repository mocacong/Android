<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="100dp">

    <ImageButton
        android:id="@+id/nav_header_profileImg"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:scaleType="centerCrop"
        android:background="@drawable/shape_circle"
        android:src="@drawable/img_no_profile"
        app:layout_constraintDimensionRatio="1:1"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageButton
        android:id="@+id/nav_header_editbtn"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:background="@color/transparent"
        android:scaleType="fitCenter"
        android:src="@drawable/img_edit_btn"
        app:layout_constraintBottom_toBottomOf="@id/nav_header_profileImg"
        app:layout_constraintEnd_toEndOf="@id/nav_header_profileImg" />

    <TextView
        android:id="@+id/nav_header_nickname"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:gravity="center"
        android:textSize="15sp"
        app:layout_constraintTop_toBottomOf="@id/nav_header_profileImg" />


</androidx.constraintlayout.widget.ConstraintLayout>